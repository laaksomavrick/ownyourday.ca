<!DOCTYPE html>
<html lang="en">
<head>
  <title>ownyourday</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="description" content="ownyourday: todo list and task scheduler"/>
  <meta name="theme-color" content="#fff">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= favicon_link_tag 'favicon.ico', rel: 'apple-touch-icon', type: 'image/png' %>
  <link rel="manifest" href="<%= asset_path 'pwa.json' %>" />

  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= yield(:head) %>
</head>

<body class="h-screen bg-white">
<main class="w-full max-w-xl mx-auto px-5 h-full">
  <% if current_user %>
    <%= render 'layouts/navigation' %>
  <% end %>
  <%= render 'layouts/alerts' %>
  <%= yield %>
</main>
</body>
</html>
